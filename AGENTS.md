# 1890 開発方針

## 目標

- `lib/engine/game/g_1890` の1890を、Windows上のローカル環境でプレイ可能な状態まで実装する。
- ゲーム仕様の根拠は `C:\Users\tgdds\Downloads\1890rule.pdf` とする。
- 要件を文書化するときは、一般的な18xxのルールで説明できる部分と1890固有のルールを分ける。
- 実装状況を記録するときは、実装済み・一部実装・未実装を区別する。

## 作業範囲

- 原則として1890に関係するコード、テスト、文書だけを変更する。
- 他タイトルの実装やテストは参考として読んでよいが、変更・追加・復元しない。
- 既存のユーザー変更や、1890と無関係な未コミット変更を保持する。
- 1890以外へ影響する共通エンジンの変更が必要な場合は、変更前に理由と影響範囲を説明する。

## 開発手順

- テスト駆動で進める。1890の期待動作をテストで表現してから、最小限の実装修正を行う。
- PDFに明記された順序、金額、会社構成、例外規則などは、可能な限り具体的な回帰テストにする。
- 他タイトルのテストを参考にした場合も、作成するテストは1890用のファイル・名前空間に置く。
- 推測でルールを確定しない。PDF、既存文書、既存コードで判断できない重要事項はユーザーに確認する。

## テスト方針

- 今後は1890のテストだけを実行する。全RSpecスイートや1890以外のタイトル専用テストは実行しない。
- 通常の検証対象は `spec/lib/engine/game/g_1890` 配下とする。
- 基本コマンド:

  ```sh
  bundle exec rspec spec/lib/engine/game/g_1890
  ```

- Windowsでは、原則として既存のDocker Compose構成を使う:

  ```powershell
  docker compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.windows.yml exec -T rack bundle exec rspec spec/lib/engine/game/g_1890
  ```

- 共通テストを実行する必要がある場合でも、1890の事例だけに限定できるものにする。限定できない全体テストは実行せず、必要性をユーザーに説明する。
- テストが長時間無出力でも、重複起動しない。プロセスとログを確認してから継続・停止を判断する。
- 1890のRSpecは `spec_helper` が `lib/engine` 全体を読み込み、全ゲーム定義のロードに約1分の固定費がかかる。テスト本体は短いため、原則として1例ごとに実行せず、関連する変更・テストを10件程度のまとまりにしてから `spec/lib/engine/game/g_1890` を実行する。
- ただし、構文エラーや明確な局所失敗の切り分けが必要な場合だけ、短い構文確認や対象を絞ったRSpecを例外的に実行してよい。

## 現在の注意事項

- 個人のローカル開発でゲーム一覧から選択してプレイできることを優先し、1890の `DEV_STAGE` は `:beta` とする。
- `:production` への変更は、完成ゲームfixtureが揃い、主要な受入れシナリオを完走できるようになるまで行わない。
- 初期会社パケットはPDF所定の順番を維持し、1890用テストで保証する。
- 1889専用テストは1890開発の検証対象に含めない。
- 1890以外の既存テスト失敗は、この作業の完了条件に含めない。

## コミット

- コミット前に1890用テストが成功することを確認する。
- コミット内容は1890の一つの仕様・修正単位にまとめ、メッセージから目的が分かるようにする。
- 各まとまりについて、先にテストを作成し、実装後に1890用テストが成功したら、そのまとまりをコミットする。
- ユーザーはコミット実行を事前許可している。1890用テスト成功後の `git add` と `git commit` は、追加確認を求めずに実行する。
